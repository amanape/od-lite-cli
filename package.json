{
  "name": "celi",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "compile": "bun build --compile --minify --sourcemap src --outfile celi",
    "celi": "bun src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.5.2"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "od-lite": "^0.3.12",
    "openai": "^4.52.1"
  }
}